services:
  iptv-checker:
    build: .
    image: iptv-checker:local
    container_name: iptv-checker
    ports:
      - "8848:8848"
    environment:
      - NODE_ENV=production
      - TZ=Asia/Shanghai
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    networks:
      - iptv-net

networks:
  iptv-net:
    driver: bridge
