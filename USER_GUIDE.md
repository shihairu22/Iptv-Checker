# IPTV Checker 使用说明

> [!NOTE]
> 本项目基于原作者 [CGG888/Iptv-Checker](https://github.com/CGG888/Iptv-Checker) 的开源项目继续维护。
> 本文档面向当前仓库版本的实际界面与功能，不移除也不替代原作者署名。

本文档重点说明这几个页面怎么用：

- 登录页
- 首页
- 结果页
- 播放器页
- 日志页

---

## 一、首次使用

### 1. 登录信息

- 默认用户名：`admin`
- 默认密码：`admin`
- 登录时需要输入验证码

建议首次登录后尽快修改管理员密码。

### 2. 运行前准备

- 确保 `ffprobe` 可执行
- 确保 `8848` 端口没有被其他旧实例占用
- 如果使用 Docker，请确认 `./data` 已挂载到容器内 `/app/data`

---

## 二、登录页

登录页主要完成身份验证。

- 用户名：输入管理员用户名
- 密码：输入管理员密码
- 验证码：点击图片可刷新
- 登录按钮：提交后进入首页

如果一直提示账号或密码错误，优先检查：

- 验证码是否过期
- 是否还在访问旧进程
- 是否曾经修改过管理员密码

---

## 三、首页

首页负责“导入 + 检测 + 快照”三件事。

### 1. 导入数据

支持三种常见方式：

- 直接粘贴文本
- 上传本地 TXT / M3U 文件
- 从远程文本地址抓取

常见文本格式：

```text
频道名称,rtp://239.0.0.1:1234
频道名称,udp://239.0.0.2:1234
频道名称,http://example.com/live.m3u8
```

### 2. UDPXY / rtp2httpd 服务器

首页可以维护 UDPXY 或 rtp2httpd 地址。

- 添加服务器
- 删除服务器
- 切换当前服务器

这个配置会影响内网模式下组播地址的代理播放基址。

### 3. CIDR 批量生成

如果你需要按地址段批量生成检测目标，可以输入 CIDR 范围后应用。

适合：

- 快速扫描某段组播地址
- 结合端口范围批量生成任务

### 4. 检测控制

首页可以直接启动批量检测。

- 开始检测：按当前列表创建任务
- 停止检测：中止任务
- 恢复检测：继续未完成任务

检测过程中系统会尝试获取：

- 是否可用
- 分辨率
- 帧率
- 码率
- 编码信息

### 5. 快照管理

首页支持版本快照操作。

- 保存：把当前频道与配置保存为快照
- 加载：恢复某个历史快照
- 删除：删除旧快照
- 刷新：刷新快照列表

如果你准备做大规模清洗、重命名或分组调整，建议先保存一个快照。

---

## 四、结果页

结果页是日常维护频道列表的主工作区。

### 1. 列表查看

可以按以下方式查看结果：

- 全部
- 在线
- 离线
- 组播 / 单播

也支持分页浏览。

### 2. 编辑频道信息

结果页可以直接编辑单条频道的元数据，包括：

- 名称
- `tvg-id`
- `tvg-name`
- Logo
- 分组
- FCC 参数
- 回看基础地址

### 3. 批量操作

结果页支持：

- 选中后批量删除
- 分页处理
- 结合搜索或筛选后集中整理

### 4. 设置与工具面板

结果页集中了项目的大部分配置入口。

#### FCC

用于维护 FCC 服务器或批量下发 `httpParam`。

#### 分组

可以维护分组名称、颜色和匹配规则。

#### 台标模板

支持维护内网台标和外网台标模板。

模板通常使用 `{name}` 作为频道名占位符。

#### 代理

支持两类代理：

- 组播代理
- 单播代理

这两类代理会影响外网模式下的导出和播放地址生成。

#### EPG

可以配置 EPG 源列表，区分内网 EPG 和外网 EPG。

#### 接口

可生成用于播放器或外部客户端的导出接口地址。

支持：

- 内网 / 外网
- 在线 / 离线 / 全部
- Token 开关

---

## 五、播放器页

播放器页用于直接播放或验证频道。

### 1. 基本功能

- 内置播放器播放
- 按频道切换
- 线路切换
- 根据内外网模式加载不同播放地址

### 2. 内网模式与外网模式

播放器会根据设置决定从哪里加载频道数据。

#### 内网模式

- 直接读取系统中的频道数据
- 组播通过 UDPXY / rtp2httpd 代理为 HTTP 地址

#### 外网模式

- 通过 `/api/export/json?scope=external` 获取频道列表
- 如果启用了 Token，播放器会自动把 token 带到 JSON 导出请求中

### 3. 台标与线路展示

播放器会尝试按当前模式拉取台标：

- 内网模式优先使用内网台标模板
- 外网模式优先使用外网台标模板

### 4. 回看与 EPG

当前版本保留了 EPG 和回看接口入口，但具体能力取决于你的数据源和配置是否完整。

如果接口返回空结果，通常说明：

- 还没有配置 EPG
- 回看链路只是占位接口
- 频道本身没有对应 EPG / catchup 参数

---

## 六、日志页

日志页用于排查运行问题。

### 1. 支持内容

- 查看日志文件列表
- 下载日志文件
- 实时流式查看日志

### 2. 适合排查的问题

- 登录失败
- 代理失败
- 外网导出失败
- 检测任务异常中断
- 播放器加载异常

### 3. 注意事项

日志页现在会对日志中的 HTML 内容做转义显示，不会把日志内容当作页面代码执行。

---

## 七、导出规则说明

### 1. 导出 M3U / TXT

结果页可直接导出：

- M3U
- TXT

导出内容会按当前频道状态和筛选条件生成。

### 2. 导出 JSON

JSON 导出主要给播放器或外部客户端使用。

#### 内网 JSON

- 更适合局域网访问
- 组播通常结合内网基址生成播放地址

#### 外网 JSON

- 更适合外部播放器
- 会结合外网基址、组播代理、单播代理生成 `httpUrl`
- 可开启 Token 校验

---

## 八、数据持久化说明

当前版本的数据主要保存在：

```text
data/iptv.db
```

数据库中包含：

- 频道数据
- 任务队列
- 配置项
- 用户信息
- 会话信息
- 快照数据

如果是从旧版本升级，程序会尝试自动迁移旧 JSON 配置到 SQLite。

---

## 九、常见问题

### 1. 为什么检测能跑，但播放不通？

常见原因：

- 组播地址本身不通
- UDPXY / rtp2httpd 地址不对
- 外网代理配置不完整
- 浏览器播放链路与检测链路走的不是同一条地址

### 2. 为什么外网模式拿不到数据？

优先检查：

- 是否开启了 Token
- Token 是否正确
- 外网基址、组播代理、单播代理是否配置正确

### 3. 为什么修改后重启没了？

先确认：

- 是否把 `data` 目录正确持久化
- 是否真的保存了设置或快照
- Docker 是否用了临时卷

### 4. 为什么日志页打不开？

当前版本已经补齐日志接口。

如果仍打不开，通常是：

- 服务没有重启到最新代码
- 旧进程还占着端口
- 登录状态无效

---

## 十、维护建议

- 大批量导入前先保存一个快照
- 外网模式上线前先用播放器页做实际验证
- 修改代理规则后，优先测试一条组播和一条单播
- 定期清理无效快照和冗余频道

---

## 十一、署名与致谢

- 原始项目作者：[CGG888/Iptv-Checker](https://github.com/CGG888/Iptv-Checker)
- 当前维护仓库：[shihairu22/Iptv-Checker](https://github.com/shihairu22/Iptv-Checker)

感谢原作者提供项目基础，也感谢所有在后续维护过程中提出问题、帮助测试和持续使用这个项目的人。

